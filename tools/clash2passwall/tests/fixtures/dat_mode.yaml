rules:
  - RULE-SET,gfw,Proxy
  - RULE-SET,Netflix,Proxy
  - GEOSITE,CN,DIRECT
  - GEOIP,CN,DIRECT
  - GEOIP,LAN,DIRECT
  - MATCH,Proxy
proxy-groups:
  - name: Proxy
    type: select
  - name: DIRECT
    type: select
rule-providers:
