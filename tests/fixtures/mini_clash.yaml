rules:
  - RULE-SET,gfw,Proxy
  - RULE-SET,proxy,Proxy
  - RULE-SET,reject,REJECT
  - RULE-SET,AI,Proxy
  - RULE-SET,telegramcidr,Proxy
  - RULE-SET,Netflix,Proxy
  - GEOSITE,CN,DIRECT
  - GEOIP,CN,DIRECT
  - GEOIP,LAN,DIRECT
  - MATCH,Proxy
proxy-groups:
  - name: Proxy
    type: select
  - name: REJECT
    type: select
rule-providers:
